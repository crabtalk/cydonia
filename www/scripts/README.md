The OG card is generated by `generate-og.mjs` during `npm run build` and when
the development server starts. Run `npm run og` from `www` to regenerate just
the image at `static/og.png`.

The first entry in the root `changelog.json` supplies the release version and
screenshot. The generator uses the video's `media.poster`, or the still image
resolved by the same media helper as the changelog page. Upload that image to
the CDN before building: a missing screenshot or failed download stops the
build rather than leaving a stale share card. Builds need network access.

The 1200 × 630 template wraps the upper half of the screenshot below the
Cydonia wordmark and headline. Both pages use a release-version query string
on the image URL so a new release has a new share-image URL.

Inter is bundled in `fonts/` for consistent rendering and is licensed under
the SIL Open Font License in `fonts/OFL.txt`. Font source:
https://github.com/google/fonts/tree/main/ofl/inter
