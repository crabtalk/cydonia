[package]
name = "cydonia-gui"
description = "GUI client for ACP agents"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "../../README.md"

[dependencies]
cydonia-core.workspace = true

# bezel
bezel.workspace = true
markdown.workspace = true
syntax.workspace = true
# `actions!` expands to literal `gpui::` paths, so the crate name has to be in
# scope even where every type is reached through the facade. Same rev, so it is
# the same crate.
gpui.workspace = true
gpui_platform.workspace = true

# crates.io
anyhow.workspace = true
dirs.workspace = true
futures.workspace = true
